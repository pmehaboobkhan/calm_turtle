# Risk Event — Data Staleness / Insufficient Indicators (EOD 2026-07-07)

- **Timestamp:** 2026-07-07T20:49:49Z (16:49 ET)
- **Routine:** end_of_day
- **Severity:** ELEVATED (operational; no capital impact)
- **Mode:** PAPER_TRADING

## What happened
The deterministic signal evaluator (`lib.signals.evaluate_all`) ran on today's close but could not compute indicators:
- `detect_regime` returned **regime = "uncertain", confidence = "low"** with **all indicators null** (`spy_above_50dma`, `spy_above_200dma`, `spy_pct_from_50dma`, vol proxy all `null`), counter-evidence: "Insufficient signal coherence".
- Root cause: today's daily bars failed to load. yfinance is TLS-blocked through the agent proxy (curl 35 / connection reset); Alpaca free-IEX daily bars are stale (standing condition observed 06-30 / 07-02 / 07-03).
- Position quotes fetched for marking were **~2784 s (~46 min) stale**, well beyond `risk_limits.yaml > data.max_data_staleness_seconds = 60`. Post-close IEX quotes were also degraded (JNJ/UNH ask = 0.0; GOOGL/SPY wide bid/ask spreads).

## Signals produced
Only one signal fired: **GLD ENTRY** from `gold_permanent_overlay` (a permanent-allocation policy that fires regardless of indicator availability). `dual_momentum_taa` and `large_cap_momentum_top5` produced no signals because their indicator inputs were null. Consolidation reported `conflict = false`, `subsumed_strategies = []`.

## Action taken (CLAUDE.md rule #5)
- **NO new entries.** GLD ENTRY → NO_TRADE, recorded at `decisions/2026-07-07/1649_GLD.json` (`final_status: NO_TRADE`, `no_trade_reason: data_staleness_rule5_insufficient_indicators`, Risk Manager verdict REJECTED).
- **All 4 open positions HELD** (GOOGL, JNJ, SPY, UNH). No EXIT signal fired on any of them. Position-health could not be re-validated on fresh bars, so positions were held (conservative — do not close on stale/degraded data absent a clean stop breach).
- Closing book was marked using today's reliable 15:30 pre_close quotes (staleness 1.4 s) rather than the degraded ~46-min-stale EOD quotes.

## Why this is not a HALT
Stale/degraded data is a reason to be MORE conservative (no new entries, hold), not to halt. No stop was cleanly breached on reliable marks; the book is up on the day. Consistent with 06-30 / 07-02 / 07-03 handling.
