# Risk Event — Data Staleness (HOLD-ALL at binding decision point)

- **Timestamp:** 2026-07-21T20:45:00Z
- **Routine:** `end_of_day` (the v1 binding paper-trade decision point)
- **Mode:** `PAPER_TRADING`
- **Severity:** Operational (capital-preserving HOLD-ALL); no trade attempted
- **Rule invoked:** CLAUDE.md #5 — "If data is stale beyond `risk_limits.yaml > data > max_data_staleness_seconds` (60s), produce NO_TRADE decisions and stamp the staleness in the journal." Also "Market data stale → NO_TRADE for affected symbols; log to `logs/risk_events/`."

## What happened

The deterministic signal evaluator (step 4) could not obtain **fresh daily bars**. `lib.data.get_bars(..., timeframe="1Day")` defaults to yfinance (the fresh source), which failed hard on every symbol (TLS reset — `curl (35) Recv failure: Connection reset by peer`), a recurring outage through the agent proxy since ~2026-06-25 (now ~26 calendar days). The evaluator fell back to `BAR_SOURCE=alpaca` (Alpaca free IEX daily bars).

- Latest Alpaca IEX daily bar for all 25 symbols ends **2026-07-02T04:00:00Z** — **~19.3 calendar days / ~14 trading sessions stale**.
- Actual staleness ≈ **1,665,792 s** vs the **60 s** `max_data_staleness_seconds` limit — **VIOLATED by ~4+ orders of magnitude.**

## Decision

- **NO_TRADE / HOLD-ALL for all fresh ENTRY candidates.** The deterministic engine fired 7 ENTRY signals (SPY, CSCO, UNH, JNJ, GOOGL, XOM, GLD), but all new entries are refused per rule #5. Six per-symbol NO_TRADE decision files were written (CSCO, UNH, JNJ, GOOGL, XOM, GLD) with `risk_manager_verdict=REJECTED` / `compliance_verdict=REJECTED` on freshness. SPY re-confirms ENTRY and is already held → HOLD, no new-entry decision needed.
- **No signal-driven EXITs.** The only open position (SPY) re-confirms ENTRY; the 17 EXIT signals are all on non-held symbols. Zero closes.
- **Real-time position health still verifiable** via Alpaca live quotes (fresh): SPY checked against stop $663.79 / target $921.94 — no breach. Zero closes.

## Circuit-breaker

- CB refresh **SKIPPED** — `paper_sim.pending_broker_count() == 7` (Guard 1: orders in flight; the 2026-05-19 spurious-HALF cause). Prior state `FULL` (throttle 1.0) carried forward from `trades/paper/circuit_breaker.json` (peak 100792.58, last observed 99962.13). No transition, no separate CB risk event, no Telegram.

## Reconciliation

- `paper_sim.reconcile()` CLEAN (1 open SPY, 0 discrepancies, alpaca-authoritative).
- Alpaca-mirror reconcile (BROKER_PAPER=alpaca): in sync (1 position matches).

## Follow-up

- Recurring: free-tier data-source fragility (yfinance blocked ~26d; Alpaca IEX daily-bar lag) keeps forcing HOLD-ALL at EOD. A fresh daily-bar source is required before the binding entry evaluation can execute any trade.
- The 7 pending broker orders have kept the circuit-breaker blind for multiple consecutive routines (Guard 1). A pending-broker finalizer / `confirm_broker_fills()` pass is needed so the breaker can advance on real equity.
