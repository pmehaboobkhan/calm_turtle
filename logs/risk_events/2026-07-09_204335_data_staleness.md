# Risk Event — Data Staleness (HOLD-ALL at binding decision point)

- **Timestamp:** 2026-07-09T20:43:35Z
- **Routine:** `end_of_day` (the v1 binding paper-trade decision point)
- **Mode:** `PAPER_TRADING`
- **Severity:** Operational (capital-preserving HOLD-ALL); no trade attempted
- **Rule invoked:** CLAUDE.md #5 — "If data is stale beyond `risk_limits.yaml > data > max_data_staleness_seconds` (60s), produce NO_TRADE decisions and stamp the staleness in the journal." Also "Market data stale → NO_TRADE for affected symbols; log to `logs/risk_events/`."

## What happened

The deterministic signal evaluator (step 4) could not obtain **fresh daily bars**. `lib.data.get_bars(..., timeframe="1Day")` routes to yfinance (the fresh source; Alpaca free IEX daily bars lag ~11 trading days). yfinance failed hard on every symbol:

```
SSLError('Failed to perform, curl: (35) Recv failure: Connection reset by peer')
```

The fallback Alpaca IEX daily bars end 2026-06-23 — **11 trading days stale**, far beyond the 60-second `max_data_staleness_seconds` threshold. No fresh close-based momentum / moving-average signals could be computed for any symbol.

This is the identical condition recorded on 2026-07-08 (daily-bar feed down → HOLD-ALL).

## Decision

- **NO_TRADE / HOLD-ALL for all watchlist symbols.** No ENTRY signals were computed (feed down), so zero new paper positions were opened. Per the no-fabricated-data rule, no per-symbol decision files were synthesized from stale/absent bars.
- **No signal-driven EXITs.** Strategy EXIT signals require the same stale bars and were not acted on.
- **Real-time position health was still verifiable** via Alpaca IEX live quotes (sub-second, fresh): all 4 open positions (GOOGL, JNJ, SPY, UNH) checked against stop/target — `portfolio_health.positions_to_close()` returned empty. No stop breached, no target hit, no invalidation. Zero closes.

## Circuit-breaker

- CB refresh **skipped** — `paper_sim.pending_broker_count() == 4` (Guard 1). Prior state `FULL` (throttle 1.0) carried forward from `trades/paper/circuit_breaker.json`. No transition, no separate CB risk event. Recurring caveat: CB blind since 2026-06-11 (pending-broker finalizer bug).

## Reconciliation

- `paper_sim.reconcile()` clean (4 open, 0 discrepancies, alpaca-authoritative).
- Alpaca-mirror reconcile (BROKER_PAPER=alpaca): in sync (4 positions match).

## Follow-up

- Recurring: free-tier data-source fragility (yfinance blocked; Alpaca IEX daily-bar lag) keeps forcing HOLD-ALL at EOD. A fresh-bar source is required before the binding entry evaluation can run.
- Land the CB pending-broker finalizer fix so the breaker stops going blind on every run.
