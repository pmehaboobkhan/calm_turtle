# Risk Event — data_staleness (end_of_day)

- **Timestamp (UTC):** 2026-07-08T20:44:51Z
- **Routine:** end_of_day (2026-07-08)
- **Mode:** PAPER_TRADING
- **Severity:** MEDIUM (capital-preserving degrade, no positions threatened)
- **CLAUDE.md rule invoked:** #5 (stale market data → NO_TRADE for affected symbols; be more conservative, never less)

## What happened

The deterministic signal evaluator (`lib.signals.evaluate_all`) could **not** be
run on today's (2026-07-08) close because the historical daily-bar feed was
unavailable:

- **yfinance:** TLS-blocked by the egress proxy (`curl (35) Recv failure:
  Connection reset by peer` / SSLError). curl_cffi does not honor the proxy CA
  bundle; requests never reach the proxy (`recentRelayFailures: []`). Recurring
  outage since ~2026-06-15.
- **Alpaca daily-bar fallback (`BAR_SOURCE=alpaca`):** reachable but free IEX
  tier lags — most recent daily bar available is **2026-07-06** (2 trading days
  stale; today's 07-08 close absent). Staleness ≫ `max_data_staleness_seconds`
  (60s) cap.

## Consequence / action taken

- **No fresh ENTRY signals** could be produced on today's close → per rule #5,
  **0 new paper entries** this run (would-be entries treated as
  NO_TRADE / data_staleness). Capital preservation.
- **EXITs assessed on FRESH real-time IEX quotes** (which are NOT subject to the
  historical-bar lag; staleness < 60s): `broker.latest_quotes_for_positions()` =
  SPY 744.01, GOOGL 347.06, JNJ 280.51, UNH 449.40.
  `lib.portfolio_health.positions_to_close()` returned **[]** — all 4 open
  positions healthy (no stop breached, no target hit, no invalidation trigger).
  **0 closes.**
- **Circuit-breaker write SKIPPED** (Guard 1): `pending_broker_count() == 4`
  (carried stuck-pending-rows finalizer bug). Prior state **FULL** (throttle 1.0,
  peak $100,792.58) carried forward unchanged; NO CB transition, NO CB risk
  event, NO CB Telegram. Diagnostic sim-vs-broker equity gap $585.49 (sim
  $101,094.97 inflated by the 4 stale pending BUY rows vs broker $100,509.48) —
  itself a symptom of the same finalizer bug, not a new event.

## Follow-ups (carried, non-URGENT)

- Wire a VIX-capable / fresh daily-bar feed (Polygon/Tiingo/paid Alpaca) — this
  outage has blocked deterministic signal generation on the close intermittently
  since mid-June and also keeps `vix_close` null for the live-trading gate.
- Land the `confirm_broker_fills` finalizer fix to clear the 4 stuck
  PENDING_BROKER rows so the circuit-breaker can re-mark equity (blind since
  06-11).
