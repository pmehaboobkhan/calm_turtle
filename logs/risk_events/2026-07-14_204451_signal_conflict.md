# Signal Conflict — GLD — 2026-07-14

**Type:** Same-symbol ENTRY+EXIT conflict (resolved deterministically; v1 does NOT auto-resolve direction)
**Detected by:** `lib/signal_consolidator.py` (EOD step 4a)
**Timestamp:** 2026-07-14T20:44:51Z (16:44 ET)
**Routine:** end_of_day
**Severity:** URGENT (per prompts/routines/end_of_day.md step 4a) — but see "Recurring / low operational impact" below.
**Mode:** PAPER_TRADING · BROKER_PAPER=alpaca

## Conflict

| Strategy | Action | Rationale |
|---|---|---|
| `gold_permanent_overlay` | ENTRY | Permanent 10% GLD allocation policy |
| `dual_momentum_taa` | EXIT | GLD below its 210d (10-month) MA |

GLD's 12m return is strong (+21.67% > cash −0.03%) but it remains **below its 10-month moving average**, so dual_momentum_taa no longer holds it (SPY is the top-1 risk asset; SPY 12m +20.12%, above 210d MA). The permanent-overlay strategy still wants GLD on policy grounds. This is the same recurring conflict logged on 2026-06-08 / 06-10 / 06-11 / 07-13; it persists into 2026-07-14.

## Resolution (per routine v1 policy)

v1 does **NOT** auto-resolve direction. The routine proceeds with both signals as normal:
- GLD is **NOT a currently open position** (closed 2026-06-08 via the dual_momentum_taa EXIT). The **EXIT** (dual_momentum_taa) is therefore a no-op — nothing to close.
- The **ENTRY** (gold_permanent_overlay) is blocked by the Risk Manager's already-flat / conflict check — no re-open while the macro-momentum strategy simultaneously signals EXIT on the same symbol.

Net effect: the GLD overlay slot stays empty. Strategy C remains under-allocated (0% vs 10% target). Overlay re-entry is evaluated on a subsequent session once the conflict clears — i.e. once GLD reclaims its 10m MA, at which point dual_momentum_taa and the overlay agree again.

## Second, independent refusal ground this run: STALE DATA

Distinct from the conflict, today's daily-bar feed is stale (yfinance TLS `curl 35`; Alpaca IEX fallback newest GLD bar 2026-07-10 (~112.7h old); regime driver SPY bar 2026-06-25/26, ~19 days — both violate `risk_limits.data.max_data_staleness_seconds=60`). Per CLAUDE.md rule #5 the GLD ENTRY is **also** refused on freshness grounds. Either ground alone forces NO_TRADE. See `decisions/2026-07-14/1644_GLD.json`.

## Recurring / low operational impact

GLD is flat and no capital moves on either leg, so this is a benign, well-understood recurring artifact of the strategy design (a permanent-overlay policy vs a trend filter that has dropped gold). It is folded into the single EOD Telegram summary rather than paged as a standalone URGENT alert, consistent with prior sessions. It will keep recurring every session until GLD reclaims its 10-month MA; not an infrastructure defect.

## Context
- Open positions at EOD (unchanged this run): GOOGL 16, SPY 20, UNH 15 (3).
- Regime (2026-07-14 deterministic, stale bars): range_bound, low confidence. SPY above 200dma and 10m SMA; below 50dma (−0.68%). Proxy-vol 16.53% annualized; VIX feed null.
- Circuit-breaker: FULL carried forward; refresh SKIPPED this run (Guard 1: 5 pending broker orders). EXITs are never CB-throttled regardless.
- No paper trade executed on GLD (or any symbol) this run.
