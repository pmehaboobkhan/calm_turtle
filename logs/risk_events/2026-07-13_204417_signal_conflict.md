# Signal Conflict — GLD — 2026-07-13

**Type:** Same-symbol ENTRY+EXIT conflict (resolved deterministically; v1 does NOT auto-resolve direction)
**Detected by:** `lib/signal_consolidator.py` (EOD step 4a)
**Timestamp:** 2026-07-13T20:44:17Z
**Routine:** end_of_day
**Severity:** URGENT (per prompts/routines/end_of_day.md step 4a)

## Conflict

| Strategy | Action | Rationale |
|---|---|---|
| `gold_permanent_overlay` | ENTRY | Permanent 10% GLD allocation policy |
| `dual_momentum_taa` | EXIT | GLD below its 210d (10-month) MA |

GLD remains **below its 10-month moving average**, so dual_momentum_taa no longer holds it (SPY is the top-1 risk asset; SPY 12m +20.86%, above 210d MA). The permanent-overlay strategy still wants GLD on policy grounds. This is the same recurring conflict logged on 2026-06-08 / 06-10 / 06-11; it persists into 2026-07-13.

## Resolution (per routine v1 policy)

v1 does **NOT** auto-resolve direction. The routine proceeds with both signals as normal:
- GLD is **NOT a currently open position** (closed 2026-06-08 via the dual_momentum_taa EXIT). The **EXIT** (dual_momentum_taa) is therefore a no-op — nothing to close.
- The **ENTRY** (gold_permanent_overlay) is blocked by the Risk Manager's already-flat / conflict check — no re-open while the macro-momentum strategy is simultaneously signalling EXIT on the same symbol.

Net effect: the GLD overlay slot stays empty. Strategy C remains under-allocated (0% vs 10% target). Overlay re-entry is evaluated on a subsequent session once the conflict clears — i.e. once GLD reclaims its 10m MA, at which point dual_momentum_taa and the overlay agree again.

## Second, independent refusal ground this run: STALE DATA

Distinct from the conflict, today's daily-bar feed is stale (yfinance TLS `curl 35`; Alpaca IEX fallback latest GLD bar 2026-07-09, ~112.7h old; regime driver SPY bar 2026-06-25, ~448h old — both violate `risk_limits.data.max_data_staleness_seconds=60`). Per CLAUDE.md rule #5 the GLD ENTRY is **also** refused on freshness grounds. Either ground alone forces NO_TRADE. See `decisions/2026-07-13/2044_GLD.json`.

## Context

- Open positions at EOD: SPY, GOOGL, JNJ, UNH (4). GLD not held.
- Regime: range_bound (low confidence). SPY above 200dma and 10m SMA but at the 50dma knife-edge (−0.053%). GLD below 210d MA — macro-momentum leadership remains in equities, not gold.
- Circuit-breaker: FULL, throttle 1.0 (CB write **SKIPPED** this run — Guard 1: 4 pending broker order(s) in flight since 2026-06-05; prior FULL state carried forward).
- Data: **STALE** — no fresh close; all new ENTRYs refused NO_TRADE per rule #5.
- Reconcile (step 8) + Alpaca mirror (step 8a): both in sync (4 positions match).

## Action items

- [x] Risk event logged (this file).
- [x] URGENT Telegram notification to be sent in step 14.
- [ ] Self-Learning Agent to attribute GLD overlay re-entry block as a distinct "conflict-blocked" outcome (weekly cadence).
