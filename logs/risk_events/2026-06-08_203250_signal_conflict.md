# Signal Conflict — GLD — 2026-06-08

**Type:** Same-symbol ENTRY+EXIT conflict (resolved deterministically; v1 does NOT auto-resolve direction)
**Detected by:** `lib/signal_consolidator.py` (EOD step 4a)
**Timestamp:** 2026-06-08T20:32:50Z
**Routine:** end_of_day
**Severity:** URGENT (per prompts/routines/end_of_day.md step 4a)

## Conflict

| Strategy | Action | Rationale |
|---|---|---|
| `gold_permanent_overlay` | ENTRY | Permanent 10% GLD allocation policy |
| `dual_momentum_taa` | EXIT | GLD below its 210d (10-month) MA |

GLD's 12m return is +27.45% (still > cash +3.93%), but it has fallen **below its 10-month moving average**, so dual_momentum_taa no longer holds it and SPY is now the top-1 risk asset (SPY 12m +25.18%, above 210d MA). The permanent-overlay strategy still wants GLD on policy grounds.

## Resolution (per routine v1 policy)

v1 does **NOT** auto-resolve the direction. The routine proceeds with both signals as normal:
- The **EXIT** (dual_momentum_taa) executes — GLD is a currently open position (36 sh @ 397.87 entry), so it is closed via `lib.paper_sim.close_position`.
- The **ENTRY** (gold_permanent_overlay) is then blocked by the Risk Manager's "already-flat / just-closed" check — no re-open the same session.

Net effect: GLD position is closed today. The permanent-overlay re-entry, if still warranted, would be evaluated on a subsequent session once the conflict clears (i.e., once GLD reclaims its 10m MA, dual_momentum_taa and the overlay agree again).

## Context

- Open positions at EOD start: CSCO, GLD, NVDA, UNH, XOM (5).
- Regime: bullish_trend (medium). SPY above 50/200dma and 10m SMA. GLD below 10m MA — the macro-momentum leadership has rotated from gold back to equities.
- Circuit-breaker: FULL, throttle 1.0 (see paired CB diagnostic this run).

## Action items

- [x] Risk event logged (this file).
- [x] URGENT Telegram notification sent.
- [ ] Self-Learning Agent to attribute GLD overlay re-entry block as a distinct "conflict-blocked" outcome (weekly cadence).
