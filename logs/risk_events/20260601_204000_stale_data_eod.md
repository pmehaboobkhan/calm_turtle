# RISK EVENT — Stale Market Data at EOD Decision Point

- **Timestamp:** 2026-06-01T20:40:00+00:00
- **Routine:** end_of_day_2026-06-01 (paper-trade decision point)
- **Mode:** PAPER_TRADING
- **Severity:** HIGH (no live order risk — outcome is NO_TRADE on all signals)

## Trigger
The EOD routine ran at ~16:39 ET (20:39 UTC) on Monday 2026-06-01, a trading
day. The deterministic signal evaluator and the watchlist data layer
(`lib.data.get_bars`, 1Day → yfinance source) return a latest daily bar dated
**2026-05-29** (Friday) for every symbol. Today's (2026-06-01) close bar is
**not present** in the feed.

- **Latest available bar:** 2026-05-29 (Friday close)
- **Age at EOD run:** ~92.7 hours (~3.86 days)
- **`risk_limits.data.max_data_staleness_seconds`:** 60
- **Staleness factor:** ~5,500x over the cap → **BREACHED**

Note: 2026-05-25 was Memorial Day (US market closed); the trading sequence is
05-27, 05-28, 05-29, then 06-01 (today). The 06-01 close is simply absent from
the data provider at run time.

## Affected symbols (all ENTRY signals this run)
GLD (dual_momentum_taa top-1, subsumes gold_permanent_overlay), CSCO, XOM,
NVDA, UNH, AMZN (large_cap_momentum_top5 ranks 1–5). All six computed on the
stale 2026-05-29 close.

## Decision
Per **CLAUDE.md hard rule #5** ("If data is stale beyond
`max_data_staleness_seconds`, produce NO_TRADE decisions and stamp the
staleness in the journal") and "Market data stale → NO_TRADE for affected
symbols; log to `logs/risk_events/`":

- All six ENTRY signals → **NO_TRADE** (`final_status: NO_TRADE`,
  reason `data_stale`). Decision files written under
  `decisions/2026-06-01/1639_<SYM>.json`.
- Risk Manager verdict: REJECTED (freshness hard-check #11).
- Compliance/Safety verdict: REJECTED (RM != APPROVED).
- No `paper_sim.open_position` calls. No LIVE_* path.

## Compounding factor — circuit-breaker could not mark equity
Independently of staleness, the Path-Z circuit-breaker equity write was
**skipped** this run because `paper_sim.pending_broker_count() == 7`: seven
stale `PENDING_BROKER` BUY breadcrumb rows (GLD/CSCO/GOOGL/XOM/WMT/UNH/NVDA,
dated 2026-05-18/19/22) have no terminal mirror-back row and, with
positions.json empty, are counted as "in flight." Per the EOD prompt, the prior
state was carried forward: **FULL, throttle 1.0**. No CB transition, no CB risk
event, no CB Telegram. This condition has persisted since 2026-05-18 and was
flagged in the 2026-05-29 daily snapshot ("Resolve 7 stale PENDING_BROKER rows
so CB write un-blocks"). It means the portfolio-drawdown safety control cannot
mark equity until the operator reconciles. This reinforces the NO_TRADE call:
opening positions while the DD safety control is blind, and while compounding
the very breadcrumb backlog that disabled it, is not capital-preserving.

## Actual ledger state (for the record — NOT a divergence)
- Local `positions.json`: `{}` (flat).
- Alpaca broker positions: `[]` (flat). Account equity $100,577.97, all cash.
- `paper_sim.reconcile()`: 0 open, 0 discrepancies, alpaca-authoritative.
- Step 8a alpaca-mirror check: **in sync** (0 positions match).
The two ledgers agree the book is flat; the 7 pending rows are stale log
artifacts, not a position-state divergence.

## Operator action recommended (NOT auto-resolved by the routine)
The stale `PENDING_BROKER` backlog requires operator reconciliation — the EOD
routine must not auto-resolve state drift (per prompt step 8a). Suggested:
`python3 scripts/sync_alpaca_state.py` to reconcile/clear stale breadcrumbs so
the CB equity write un-blocks on the next run. Separately, investigate why the
06-01 daily close is not present in the data feed at EOD (provider lag /
session-not-yet-settled) so tomorrow's run has a fresh close to act on.

## No order risk
Mode is PAPER_TRADING and the outcome is NO_TRADE on every signal. No live
order path was reachable at any point.
