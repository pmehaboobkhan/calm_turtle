# Risk Event — Same-Symbol Signal Conflict (GLD)

- **Timestamp:** 2026-07-21T20:45:01Z
- **Routine:** `end_of_day`
- **Mode:** `PAPER_TRADING`
- **Severity:** Informational / recurring (no position exposure); URGENT-class per routine step 4a
- **Detector:** `lib.signal_consolidator.consolidate` (step 4a) — `conflict=True` on GLD.

## What happened

The signal consolidator flagged **GLD** with `conflict=True`:

- `gold_permanent_overlay` → **ENTRY** (permanent 10% GLD allocation policy; independent of MA status).
- `dual_momentum_taa` → **EXIT** (GLD is below its 210-day MA).

An ENTRY and an EXIT on the same symbol from two different strategies. Per end_of_day routine step 4a, v1 does **NOT** auto-resolve: the EXIT would execute if a position existed; the ENTRY would then be blocked by the risk manager's already-flat check.

## Resolution this run

- **GLD is NOT currently held** (positions.json holds only SPY). The EXIT leg therefore has no position to close, and the ENTRY leg is a fresh open → **NO_TRADE on stale data** (see paired `logs/risk_events/2026-07-21_204500_data_staleness.md`). Net effect: **no action, no exposure change.**
- Consolidator `primary_strategy` = `gold_permanent_overlay`; recorded in `decisions/2026-07-21/1645_GLD.json` under `consolidation`.
- This is the **recurring** GLD A/C conflict (also logged 07-13, 07-16). It repeats every run in which GLD sits below its 210d MA while the permanent overlay still fires ENTRY.

## Follow-up

- Consider a deterministic tie-break in `signal_consolidator` so the permanent-overlay ENTRY and the TAA below-210d EXIT stop producing a daily conflict event when GLD is flat and stale (proposal only — no config change here).
