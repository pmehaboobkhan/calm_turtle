# Prediction Reviews — 2026-07-07 (end_of_day)

> Predictions/observations from `end_of_day_2026-07-07` (~16:49 ET). Mode
> `PAPER_TRADING` (learning writes permitted). 0 opens, 0 closes. 1 NO_TRADE
> (GLD, rule #5). CB FULL (write skipped, Guard 1, 4 pending). Reconcile
> deliberately skipped (broker filled-position view empty → would wipe);
> Alpaca-mirror DIVERGENCE logged as the known pending-broker artifact.

## Resolutions of prior open predictions (2026-07-03, window 2026-07-06)
The 2026-07-06 EOD did not finalize (no EOD section / snapshot for 07-06); the
07-03 predictions carried a 07-06 window and are resolved here on first available
EOD evidence (07-07):

- **P1 — daily-bar feed still broken:** ✅ CONFIRMED. 07-07 EOD again failed to
  load daily bars (yfinance TLS-blocked; IEX daily bars stale); regime engine
  returned uncertain/low with null indicators.
- **P2 — CB stays FULL:** ✅ CONFIRMED. CB FULL carried; no transition. Book
  marginally above frozen peak $100,792.58 (carry DD ≈ 0.0%).
- **P3 — pending-broker chain persists absent operator action:** ✅ CONFIRMED,
  with a sharper finding — `confirm_broker_fills()` WAS run today (the prescribed
  cleanup) but resolved nothing (`pending_broker_count()` 4→4) due to a status-
  string normalization bug. The chain persists NOT for lack of running the
  finalizer but because the finalizer is broken. See
  `prompts/proposed_updates/2026-07-07_confirm_broker_fills_status_normalization_bug.md`.
- **P4 — GOOGL holds above its stop:** ✅ CONFIRMED. GOOGL 368.20 vs 331.68 stop
  (+11.0% cushion); did not breach.

## Prediction 1 — pending-broker suppression persists until the finalizer fix lands
- **Prediction (conf 0.9):** `pending_broker_count()` stays at 4 (CB writes + safe
  reconcile disabled) on the next EOD unless a human PR lands the
  `confirm_broker_fills()` status-normalization fix (or an operator manually
  finalizes the rows). Re-running the finalizer unchanged will not help.
- **Resolution window:** next EOD after any PR merge touching `lib/paper_sim.py`.

## Prediction 2 — daily-bar feed still broken next session
- **Prediction (conf 0.8):** 2026-07-08 EOD again evaluates on stale/failed daily
  bars (regime uncertain or stale) unless a proxy-reachable daily-bar feed is
  wired; new ENTRY signals stay NO_TRADE under rule #5.
- **Resolution window:** 2026-07-08 EOD.

## Prediction 3 — circuit-breaker stays FULL
- **Prediction (conf 0.9):** CB remains FULL through 2026-07-08; no HALF
  transition. Book is at/above the frozen peak; ~8pp of cushion to the 8% HALF
  trigger even if a CB write resumes.
- **Resolution window:** next session with a written CB mark.

## Prediction 4 — all 4 holds stay above their stops
- **Prediction (conf 0.65):** GOOGL (thinnest, +11.0% above stop), SPY, JNJ, UNH
  do not breach their stops on the next fresh session; no EXIT signal expected
  absent a fresh regime read. GOOGL is the one to watch (only underwater name).
- **Resolution window:** 2026-07-08 EOD (fresh-data session).

## Prediction 5 — no destructive reconcile is safe while broker view is empty/flapping
- **Observation (conf 0.85):** the broker filled-position view flapped (4 at
  pre_close 07-07 → empty at EOD 07-07); any `reconcile()` / `--reset-fresh-start`
  during an empty read will wipe the 4 real positions. Standing hazard until the
  finalizer + reconcile-guard fixes both land.
- **Resolution window:** ongoing; re-affirm each EOD until resolved.
