# Prediction Reviews — 2026-07-08 (end_of_day)

> Predictions/observations from `end_of_day_2026-07-08` (~16:44 ET). Mode
> `PAPER_TRADING` (learning writes permitted). 0 opens, 0 closes, 0 fresh
> signals (daily-bar feed down → rule #5 HOLD-ALL). CB FULL (write skipped,
> Guard 1, 4 pending). Reconcile clean (broker view populated, 4/4 match);
> Alpaca-mirror in sync. Portfolio +0.51% since inception.

## Resolutions of prior open predictions (from 2026-07-07, window 2026-07-08 EOD)

- **P1 — pending-broker suppression persists until finalizer fix lands (conf 0.9):**
  ✅ CONFIRMED. `pending_broker_count()` still 4; no PR merged touching
  `lib/paper_sim.py`. CB write skipped again (Guard 1); sim equity inflated
  ~$585 vs broker.
- **P2 — daily-bar feed still broken next session (conf 0.8):** ✅ CONFIRMED.
  2026-07-08 EOD could not evaluate signals on today's close — yfinance
  TLS-blocked (curl 35); Alpaca daily bars lag to 07-06. New entries NO_TRADE
  under rule #5.
- **P3 — circuit-breaker stays FULL through 2026-07-08 (conf 0.9):** ✅ CONFIRMED.
  CB FULL carried; no HALF transition. Broker equity $100,509.48 vs frozen peak
  $100,792.58 → DD ≈ 0.28%, ~7.7pp of cushion to the 8% HALF trigger.
- **P4 — all 4 holds stay above their stops (conf 0.65):** ✅ CONFIRMED. On fresh
  real-time quotes none breached: GOOGL 347.06 vs 331.68 stop (+4.6% cushion,
  still the thinnest / only underwater name), SPY 744.01, JNJ 280.51, UNH 449.40.
  No EXIT signal fired; `positions_to_close()` = [].
- **P5 — no destructive reconcile is safe while broker view empty/flapping
  (conf 0.85):** ⚖️ PARTIAL / hazard did not trigger today. The broker
  filled-position view was POPULATED (4 positions) this run, so `reconcile()` was
  safe and ran clean (alpaca-authoritative, 4/4 match) and the mirror was in
  sync. The flap seen 07-07 (4→empty) did not recur today. Standing hazard
  re-affirmed until the finalizer + reconcile-guard fixes both land, but it was
  dormant this session.

## Prediction 1 — pending-broker suppression persists until the finalizer fix lands
- **Prediction (conf 0.9):** `pending_broker_count()` stays at 4 (CB writes stay
  skipped) on the next EOD unless a human PR lands the `confirm_broker_fills()`
  status-normalization fix or an operator manually finalizes the rows.
- **Resolution window:** next EOD after any PR merge touching `lib/paper_sim.py`.

## Prediction 2 — daily-bar feed still broken next session
- **Prediction (conf 0.8):** 2026-07-09 pre_market / EOD again evaluate on
  stale/failed daily bars unless a proxy-reachable, fresh daily-bar feed is
  wired; new ENTRY signals stay NO_TRADE under rule #5. Real-time IEX quotes
  remain available for exit/health checks.
- **Resolution window:** 2026-07-09 EOD.

## Prediction 3 — circuit-breaker stays FULL
- **Prediction (conf 0.9):** CB remains FULL through 2026-07-09; no HALF
  transition. DD from frozen peak ≈ 0.3%; ~7.7pp cushion to the 8% HALF trigger
  even if a CB write resumes.
- **Resolution window:** next session with a written CB mark.

## Prediction 4 — all 4 holds stay above stops; JNJ approaches its take-profit
- **Prediction (conf 0.6):** GOOGL (thinnest, +4.6% above stop), SPY, JNJ, UNH do
  not breach stops on the next fresh session. NEW watch: JNJ at +20.5% is ~3.7%
  below its +25% take-profit ($290.96) — a further JNJ leg up could trigger a
  target-hit CLOSE on a future close. GOOGL remains the only underwater name.
- **Resolution window:** 2026-07-09 EOD (fresh-data session).
