# Prediction Reviews — 2026-07-14 (end_of_day)

> Predictions/observations from `end_of_day_2026-07-14` (~16:44 ET). Mode
> `PAPER_TRADING` (learning writes permitted). 0 opens, 0 closes this EOD run
> (JNJ was closed earlier today by pre_close for overnight earnings risk, est.
> +$538.98). 0 actionable fresh signals — daily-bar feed down → rule #5
> HOLD-ALL. CB FULL (write skipped, Guard 1, 5 pending). Reconcile clean (3/3
> match); Alpaca-mirror in sync. Broker equity $100,385.06 → +0.39% since
> inception; day +$15.53 (+0.02% close-to-close). Recurring GLD A/C
> signal-conflict logged; pending-broker escalation logged.

## Resolutions of prior open predictions (from 2026-07-13, window = this EOD)

- **P1 — pending-broker suppression persists until finalizer fix lands (conf 0.9):**
  ✅ CONFIRMED. `pending_broker_count()` now 5 (grew as pre_close's JNJ close
  added a row); no PR merged touching the fill finalizer. CB write skipped again
  (Guard 1) every routine 07-14 (midday, pre_close, EOD).
- **P2 — daily-bar feed still broken into 2026-07-14 (conf 0.8):** ✅ CONFIRMED.
  yfinance TLS-blocked (curl 35) across all symbols again; Alpaca IEX daily bars
  lag (newest 2026-07-10; SPY 300-bar fetch ends 2026-06-25/26). All new ENTRYs
  NO_TRADE under rule #5. Real-time IEX quotes remained available for health.
- **P3 — circuit-breaker stays FULL (conf 0.9):** ✅ CONFIRMED. CB FULL carried;
  no HALF transition. Broker equity $100,385.06 vs frozen peak $100,792.58 →
  DD ≈ 0.40%, ~7.6pp cushion to the 8% HALF trigger.
- **P4 — all holds stay above stops; JNJ shy of take-profit (conf 0.6):**
  ✅ CONFIRMED (with action). No stop breach on any held name. JNJ never hit its
  $290.96 target — instead it was closed 07-14 pre_close (+8.9% est.) for the
  07-15 earnings binary, not on a price trigger. GOOGL remains the thinnest held
  name (−2.7% to −4.4% on live marks, ~7% above stop).
- **P5 — post-close IEX quote basis-gap is a feed artifact, not real MTM
  (conf 0.75):** ✅ CONFIRMED (smaller today). Broker view is consistent this
  run (reconcile + mirror clean, 3 positions match); the sim-vs-broker equity
  gap that has flapped on prior sessions did not force a destructive reconcile.

## Prediction 1 — pending-broker suppression persists until the finalizer fix lands
- **Prediction (conf 0.9):** `pending_broker_count()` stays ≥ 5 (CB writes stay
  skipped, Guard 1) on the next EOD unless a human PR lands the
  `confirm_broker_fills()` status-normalization fix (`"OrderStatus.FILLED"` →
  `"filled"`) or an operator manually finalizes the stale PENDING_BROKER rows.
- **Resolution window:** next EOD after any PR merge touching `lib/paper_sim.py`
  or `lib/broker.py`, or after a manual operator finalization.

## Prediction 2 — daily-bar feed still broken into 2026-07-15
- **Prediction (conf 0.8):** 2026-07-15 pre_market / pre_close / EOD again
  evaluate on stale/failed daily bars unless a proxy-reachable fresh daily-bar
  feed is wired; new ENTRY signals stay NO_TRADE under rule #5. Real-time IEX
  quotes stay available for held-position health checks.
- **Resolution window:** next routine that fetches daily bars.

## Prediction 3 — circuit-breaker stays FULL next session
- **Prediction (conf 0.9):** CB remains FULL with no transition next session.
  Even if equity could be written, DD ≈ 0.40% is ~7.6pp below the 8% HALF
  trigger and the book is only 3 long positions; a same-session move to HALF
  would require a ~$7.6k drawdown. Guard 1 will most likely keep the write
  skipped regardless.
- **Resolution window:** next EOD CB step.

## Prediction 4 — UNH becomes next-trading-day earnings exposure at pre_close 07-15
- **Prediction (conf 0.85):** UNH (held 15 sh) reports 07-16 BMO, so at
  pre_close 07-15 it enters the 1-day earnings-caution window and will be
  flagged for an overnight-risk close (mirroring today's JNJ handling), absent a
  stop/target trigger before then. UNH sits ~+6.6% vs entry, well above its
  $356.82 stop.
- **Resolution window:** pre_close 2026-07-15.

## Prediction 5 — held book stays above stops overnight 07-14 → 07-15
- **Prediction (conf 0.6):** SPY / GOOGL / UNH all remain above their stops on
  the 07-15 open; GOOGL is the thinnest buffer (~7% above stop) but no scheduled
  07-15 single-name catalyst for it (earnings 07-22). SPY faces no fresh
  scheduled tier-A macro release for 07-15 per pre_close's macro scan.
- **Resolution window:** 2026-07-15 pre_market / market_open health check.

## Prediction 6 — GLD A/C conflict recurs next session
- **Prediction (conf 0.85):** the gold_permanent_overlay(ENTRY) vs
  dual_momentum_taa(EXIT) GLD conflict recurs at the next EOD, keeping Strategy C
  at 0% vs its 10% target, until GLD closes back above its 210d/10-month MA.
- **Resolution window:** next EOD signal consolidation (step 4a).
