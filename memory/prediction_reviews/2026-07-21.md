# Prediction Reviews — 2026-07-21 (end_of_day)

> Predictions/observations from `end_of_day_2026-07-21` (~16:45 ET). Mode
> `PAPER_TRADING` (learning writes permitted). 0 opens, 0 closes this EOD run —
> daily-bar feed still down → CLAUDE.md rule #5 HOLD-ALL. Deterministic engine
> fired 7 ENTRY (SPY, CSCO, UNH, JNJ, GOOGL, XOM, GLD); all new entries NO_TRADE
> on stale bars (Alpaca IEX ends 2026-07-02, ~19.3d / ~1,665,792s vs 60s limit).
> Only open name SPY (20 @ 743.78) re-confirmed TAA top-1 ENTRY → HOLD, no EXIT.
> CB FULL (write skipped, Guard 1, 7 pending broker orders). Reconcile clean
> (1/1 SPY, alpaca-authoritative); Alpaca-mirror in sync. Broker EOD equity
> $99,998.56; day +$36.43 (+0.04% close-to-close vs prior $99,962.13); SPY
> unrealized +$84.03 (+0.56%). Drawdown 0.79% from frozen peak $100,792.58.
> Recurring GLD A/C signal-conflict logged
> (`logs/risk_events/2026-07-21_204501_signal_conflict.md`). Regime bullish_trend
> / medium (SPY +1.01% above 50dma) on the same stale bars.

## Resolutions of prior open predictions (from 2026-07-16, window = this EOD)

- **P1 — pending-broker suppression persists until finalizer fix lands (conf 0.9):**
  ✅ CONFIRMED. `pending_broker_count()` now **7** (was 6; the 2026-07-17 GOOGL
  news-close added another unconfirmed row). No PR merged touching the fill
  finalizer. CB write skipped again (Guard 1) this run.
- **P2 — daily-bar feed still broken into next session (conf 0.8):** ✅ CONFIRMED.
  yfinance TLS-blocked (curl 35) across all symbols on the fresh EOD fetch (~26d
  outage); Alpaca IEX daily bars end 2026-07-02 (~19.3d, unchanged since the
  07-21 pre-market). All new ENTRYs NO_TRADE under rule #5. Real-time IEX quotes
  remained available for SPY marks.
- **P3 — circuit-breaker stays FULL (conf 0.9):** ✅ CONFIRMED. CB FULL carried;
  no transition. Diagnostic DD ≈ 0.79% (equity $99,998.56 vs frozen peak
  $100,792.58), ~7.2pp cushion to the 8% HALF trigger.
- **P4 — UNH re-entry stays refused through the 07-16 earnings event (conf 0.7):**
  ✅ CONFIRMED. UNH re-entry REFUSED at this EOD
  (`decisions/2026-07-21/1645_UNH.json`) on staleness; the 07-16 BMO earnings are
  now past but invisible to the 07-02 bars, so no fresh post-earnings close exists
  yet. The re-entry decision proper remains deferred to the first fresh EOD.
- **P5 — routine-log archiver keeps erroring until the malformed file is fixed
  (conf 0.85):** ✅ CONFIRMED. `scripts/archive_routine_logs.py` again exited
  non-zero (non-fatal) on the same legacy filename
  `2026-06-01_0935_market_open_noop.md`; no rename/removal PR landed.

## Prediction 1 — pending-broker suppression persists until the finalizer fix lands
- **Prediction (conf 0.9):** `pending_broker_count()` stays ≥ 7 (CB writes stay
  skipped, Guard 1) on the next EOD unless a human PR lands the fill finalizer /
  `confirm_broker_fills()` pass. The breaker has now been blind for 4+ consecutive
  routines; peak-tracking on real equity cannot resume until the backlog clears.
- **Resolves:** next EOD run.

## Prediction 2 — daily-bar feed still broken next session
- **Prediction (conf 0.75):** yfinance remains TLS-reset through the agent proxy
  and Alpaca IEX daily bars remain stuck at/near 2026-07-02 on the next EOD fetch,
  forcing another rule #5 HOLD-ALL for all fresh ENTRYs. Lower confidence than P1
  because a proxy/feed fix could land any day after ~26d of outage.
- **Resolves:** next EOD run.

## Prediction 3 — circuit-breaker stays FULL
- **Prediction (conf 0.9):** CB state remains FULL (no FULL→HALF transition). DD
  from the $100,792.58 frozen peak stays well inside the 8% HALF trigger (~0.8%
  now; would need a ~$7.3k equity drop to breach). Barring a large gap on fresh
  data, no throttle engages.
- **Resolves:** next EOD run.

## Prediction 4 — GOOGL re-entry refused through the 07-22 earnings event
- **Prediction (conf 0.7):** GOOGL is NOT re-opened at the next EOD. It sits inside
  the `holding_earnings_caution_window_days=1` window for its 2026-07-22 AMC print,
  and the data feed is stale — both independently force NO_TRADE. Even on fresh
  data, a fresh open on the eve of / day after a binary print is disfavored.
- **Resolves:** next 1-2 EOD runs (post-earnings).

## Prediction 5 — routine-log archiver keeps erroring
- **Prediction (conf 0.85):** `scripts/archive_routine_logs.py` continues to exit
  non-zero (non-fatal) on `2026-06-01_0935_market_open_noop.md` until a human PR
  renames/removes the malformed log file.
- **Resolves:** next EOD run.

## Prediction 6 — SPY held; TAA anchor unchanged absent fresh trend break
- **Prediction (conf 0.85):** SPY (20 @ 743.78) remains held at the next EOD. It
  re-confirms dual_momentum_taa top-1 (12m +20.6% > SHV; above 50/200/10m MAs on
  stale bars). Only a fresh daily close showing SPY below its 10-month MA — or a
  stop breach at $663.79 (~11% below spot) — would flip it to EXIT. Neither is
  in view on current data.
- **Resolves:** next EOD run (or first fresh-data EOD).
