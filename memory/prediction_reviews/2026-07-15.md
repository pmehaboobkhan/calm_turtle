# Prediction Reviews — 2026-07-15 (end_of_day)

> Predictions/observations from `end_of_day_2026-07-15` (~16:44 ET). Mode
> `PAPER_TRADING` (learning writes permitted). 0 opens, 0 closes this EOD run
> (UNH was closed earlier today by pre_close for 07-16 overnight earnings risk;
> realized +$293.81, confirmed vs Alpaca fill 418.2613). 0 actionable fresh
> signals — daily-bar feed still down → rule #5 HOLD-ALL. CB FULL (write
> skipped, Guard 1, 6 pending). Reconcile clean (2/2 match); Alpaca-mirror in
> sync. Broker EOD equity $100,384.62 → +0.38% since inception; day −$39.44
> (−0.04% close-to-close). Recurring GLD A/C signal-conflict logged. New
> anomaly: routine-log archiver failing on a malformed legacy filename.

## Resolutions of prior open predictions (from 2026-07-14, window = this EOD)

- **P1 — pending-broker suppression persists until finalizer fix lands (conf 0.9):**
  ✅ CONFIRMED. `pending_broker_count()` now **6** (grew 5→6 as pre_close's UNH
  close added a row); no PR merged touching the fill finalizer. CB write skipped
  again (Guard 1) on every 07-15 routine.
- **P2 — daily-bar feed still broken into 2026-07-15 (conf 0.8):** ✅ CONFIRMED.
  yfinance TLS-blocked (curl 35) across all symbols on the fresh EOD fetch;
  Alpaca IEX daily bars lag (candidates newest 2026-07-10; SPY 300-bar window
  ends 2026-06-29). All new ENTRYs NO_TRADE under rule #5. Real-time IEX quotes
  remained available for marks.
- **P3 — circuit-breaker stays FULL (conf 0.9):** ✅ CONFIRMED. CB FULL carried;
  no transition. Diagnostic DD ≈ 0.40% (equity $100,384.62 vs frozen peak
  $100,792.58), ~7.6pp cushion to the 8% HALF trigger.
- **P4 — UNH becomes next-trading-day earnings exposure at pre_close 07-15
  (conf 0.85):** ✅ CONFIRMED (with action). UNH (15 sh) entered the 1-day
  earnings-caution window at pre_close 07-15 (Q2 07-16 BMO) and was CLOSED there;
  EOD confirmed the fill (SELL 15 @ 418.2613, +$293.81) and REFUSED re-entry
  (`2044_UNH.json`) on both staleness and the still-open earnings window.
- **P5 — post-close IEX quote basis-gap is a feed artifact, not real MTM
  (conf 0.75):** ✅ CONFIRMED. Broker view consistent this run (reconcile +
  mirror clean, 2 positions match); no destructive reconcile forced.

## Prediction 1 — pending-broker suppression persists until the finalizer fix lands
- **Prediction (conf 0.9):** `pending_broker_count()` stays ≥ 6 (CB writes stay
  skipped, Guard 1) on the next EOD unless a human PR lands the
  `confirm_broker_fills()` status-normalization fix (`"OrderStatus.FILLED"` →
  `"filled"`) or an operator manually finalizes the stale PENDING_BROKER rows.
- **Resolution window:** next EOD after any PR merge touching `lib/paper_sim.py`
  or `lib/broker.py`, or after a manual operator finalization.

## Prediction 2 — daily-bar feed still broken into 2026-07-16
- **Prediction (conf 0.8):** 2026-07-16 routines again evaluate on stale/failed
  daily bars unless a proxy-reachable fresh daily-bar feed is wired; new ENTRY
  signals stay NO_TRADE under rule #5. Real-time IEX quotes stay available for
  held-position health.
- **Resolution window:** next routine that fetches daily bars.

## Prediction 3 — circuit-breaker stays FULL next session
- **Prediction (conf 0.9):** CB remains FULL with no transition next session.
  DD ≈ 0.40% is ~7.6pp below the 8% HALF trigger and the book is only 2 long
  positions; a same-session move to HALF would require a ~$7.6k drawdown, and
  Guard 1 will most likely keep the write skipped regardless.
- **Resolution window:** next EOD CB step.

## Prediction 4 — UNH re-entry stays refused through the 07-16 earnings event
- **Prediction (conf 0.7):** UNH will NOT be re-opened until (a) daily bars are
  fresh AND (b) the 07-16 BMO earnings clears the 1-day caution window. If the
  post-earnings fresh close keeps UNH in the top-5, a fresh ENTRY is reconsidered
  at a subsequent EOD; if it gaps out of the top-5, no re-entry.
- **Resolution window:** first EOD with fresh post-earnings bars (≥ 2026-07-16 EOD).

## Prediction 5 — routine-log archiver keeps erroring until the malformed file is fixed
- **Prediction (conf 0.85):** `scripts/archive_routine_logs.py` keeps exiting
  non-zero every routine (non-fatal) until a human renames/removes
  `logs/routine_runs/2026-06-01_0935_market_open_noop.md` to the expected
  `YYYY-MM-DD_HHMMSS_<routine>_<phase>.md` pattern. Because it aborts on the
  first bad name, other >30-day logs are not being archived either.
- **Resolution window:** next routine after the file is renamed/removed or the
  archiver is made tolerant of legacy names.
