# Prediction Reviews — 2026-07-01 (market_open)

> Predictions/resolutions from `market_open_2026-07-01` (~09:39 ET, market OPEN).
> Mode `PAPER_TRADING` (learning writes permitted). Monitoring-only routine: no
> new entries. Operative decisions this run: **2 PAPER_CLOSE executed** (NVDA,
> CSCO — deterministic stop-loss exits, full gate chain APPROVED). CB refresh
> SKIPPED (Guard 1, pending broker order). Reconcile clean (5==5, discrepancies=[]).

## Resolution — 06-30 EOD "next-session data freshness"
- **Predicted (conf 0.8):** next routine again hits stale daily bars absent a
  proxy-reachable fresh feed.
- **Outcome:** CONFIRMED (bars) / MITIGATED (quotes). yfinance bar feed still
  TLS-blocked; Alpaca free-IEX daily bars still lag to 06-12. BUT because the
  market was OPEN at 09:39 ET, live Alpaca IEX **quotes and last-trades were
  fresh** (< 3s staleness), which — unlike the 06-30 post-close 37-min-stale
  read — allowed the CSCO/NVDA stop status to finally be resolved on trustworthy
  executed prints. Bar path remains broken; quote path was usable intraday.

## Resolution — 06-30 EOD Prediction 4 / CSCO stop proximity (deferred)
- **Prior state (conf 0.7, INDETERMINATE on 06-30):** CSCO stop status could not
  be confirmed on stale data; last fresh read was 117.58 (above stop).
- **Outcome:** RESOLVED → **CLOSED**. On fresh live prints CSCO straddled the
  117.00 stop within ±0.25% (116.85–117.26); the majority of clean tight-spread
  prints were below 117.00 and `lib.portfolio_health` flagged the breach. Full
  gate chain (trade_proposal → risk_manager APPROVED → compliance_safety
  APPROVED). PAPER_CLOSE executed 46 sh @ ~117.18, est. realized −$304 (−5.3%).
  Note: a marginal/borderline call — the earlier 116.38 sub-stop print was on a
  dislocated ~5.4% spread; decision honored the pre-committed stop per
  capital-preservation discipline. `decisions/2026-07-01/0939_CSCO.json`.

## Resolution — 06-30 XOM stop-breach handling (deferred)
- **Prior state (DEFERRED on 06-30):** XOM stop resolution deferred to next fresh
  session.
- **Outcome:** RESOLVED → **HELD (no breach on fresh data)**. Fresh last-trade
  ~137.4–137.5 vs 136.84 stop — XOM is *above* its stop on clean prints (the
  06-30 sub-stop read was stale/degraded). `portfolio_health` did NOT flag XOM.
  No close. XOM sits just above stop; continues on watch.

## Resolution — 06-30 EOD "circuit-breaker stays FULL next session"
- **Predicted (conf 0.9):** CB stays FULL; no HALF transition.
- **Outcome:** CONFIRMED (state) / NOT-RE-MARKED (write). CB write itself was
  Guard-1 SKIPPED again (pending broker count == 1 at step 6), so FULL was
  carried forward without a fresh equity mark. No transition; buffer to the 8%
  HALF trigger remains wide (broker equity ~$100,062 vs peak $100,792.58, DD
  ~0.72% if marked — nowhere near 8%).

## Resolution — 06-30 EOD "stuck pending-broker counter"
- **Predicted (conf 0.85):** count stays 1, keeps suppressing CB writes, until
  operator intervention.
- **Outcome:** CONFIRMED at run start (count == 1, CB write skipped). Count then
  rose to **3** after this run's two CLOSE log rows were appended (reconcile is
  alpaca-authoritative on positions but does not rewrite append-only log status
  strings). Still requires operator/EOD cleanup; now blocking CB writes AND
  inflating the counter.

## Prediction 1 — NVDA close is durable (not a bad-tick exit)
- **Prediction:** NVDA's stop breach (~194.5 vs 196.79, ~1.1% below, tight
  ~0.02–0.4% spreads) is a genuine trend break, not an open-liquidity wick; NVDA
  does NOT reclaim 196.79 and hold today. Close was the correct risk-reducing
  action; est. realized −$391 (−6.9%).
- **Confidence:** 0.85.
- **Outcome:** PENDING (resolves at EOD 07-01 / next session close).

## Prediction 2 — CSCO close was the right call at the margin
- **Prediction:** CSCO does NOT durably reclaim and hold ≥ 117.00 on clean
  prints through today; i.e., closing at ~117.18 did not exit a still-valid
  position. Given the ±0.25% straddle this is the lowest-confidence decision of
  the run and explicitly flagged for outcome review.
- **Confidence:** 0.6 (borderline; dislocated-spread noise made the level hard
  to read).
- **Outcome:** PENDING (resolves at EOD 07-01 close — was CSCO above or below
  117.00 on the official close?).

## Prediction 3 — reconcile stays clean post-close
- **Prediction:** local vs Alpaca position parity holds (5==5) through EOD; no
  reconcile discrepancy from the two closes. The 3-count pending-broker figure is
  a log-status artifact, not a real position mismatch.
- **Confidence:** 0.85.
- **Outcome:** PENDING (resolves at EOD 07-01 reconcile).

## New prediction — stuck pending-broker order persists
- **Prediction:** without operator/EOD cleanup, `pending_broker_count()` stays ≥ 3
  and continues to suppress CB writes at the next routine (Guard 1), leaving CB
  state carried-forward rather than freshly marked.
- **Confidence:** 0.8.
- **Outcome:** PENDING (resolves at next routine run).

---

# Prediction Reviews — 2026-07-01 (pre_close)

> Predictions/resolutions from `pre_close_2026-07-01` (~15:33 ET, market OPEN).
> Mode `PAPER_TRADING` (learning writes permitted). Monitoring-only routine: no
> new entries. Operative decision this run: **1 PAPER_CLOSE executed (XOM —
> deterministic stop-loss exit, full gate chain APPROVED)**; 4 held overnight.
> CB refresh SKIPPED (Guard 1, pending_broker_count==3). Reconcile clean (4==4,
> discrepancies=[]). Overnight-risk overlay: earnings NONE, macro NONE next session.

## Resolution — midday "XOM first fresh sub-stop print triggers PAPER_CLOSE"
- **Prior state (WATCH at midday, XOM 137.05 vs 136.84, +0.21 above):** expected a
  first fresh sub-stop print at pre-close to trigger a close; warned not to act on
  dislocated-spread ticks.
- **Outcome:** RESOLVED → **CLOSED.** At pre-close XOM printed bid 135.89 / ask
  135.93 — BOTH below the 136.84 stop, ~0.69% under, on a tight ~0.03% clean
  spread (not dislocated). `portfolio_health` flagged stop_breached=TRUE,
  positions_to_close=[XOM]. Full gate chain (trade_proposal → risk_manager
  APPROVED → compliance_safety APPROVED). PAPER_CLOSE executed 26 sh @ 135.89,
  Alpaca order FILLED, est. realized −$419.01 (−10.6%). `decisions/2026-07-01/1534_XOM.json`.

## Prediction 1 — XOM close is durable (stop-break, not intraday wick)
- **Prediction:** XOM does NOT reclaim and hold ≥ 136.84 into the 07-01 close;
  the clean two-sided sub-stop break (135.89/135.93) was a genuine stop violation,
  and honoring the pre-committed stop was the correct capital-preserving action.
- **Confidence:** 0.8.
- **Outcome:** PENDING (resolves at EOD 07-01 close — was XOM above or below 136.84 at the official close?).

## Prediction 2 — 4 overnight holds carry no realized overnight-risk event
- **Prediction:** GOOGL/JNJ/SPY/UNH held overnight see NO next-session gap-driven
  invalidation from an earnings surprise or macro shock, consistent with the
  overlay verdict (no next-trading-day earnings; no Calendar-A macro on 07-02;
  NFP is 07-03). None breach its stop at the 07-02 open.
- **Confidence:** 0.75.
- **Outcome:** PENDING (resolves at 07-02 market_open).

## Prediction 3 — pending-broker counter keeps rising, still suppresses CB
- **Prediction:** without operator/EOD cleanup, `pending_broker_count()` stays ≥ 4
  (now 4 after XOM's CLOSE row) and continues to Guard-1-suppress CB writes at EOD.
- **Confidence:** 0.8.
- **Outcome:** PENDING (resolves at EOD 07-01).
