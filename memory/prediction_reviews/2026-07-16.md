# Prediction Reviews — 2026-07-16 (end_of_day)

> Predictions/observations from `end_of_day_2026-07-16` (~16:45 ET). Mode
> `PAPER_TRADING` (learning writes permitted). 0 opens, 0 closes this EOD run —
> daily-bar feed still down → rule #5 HOLD-ALL. Both held names (SPY 20,
> GOOGL 16) re-confirmed ENTRY / no EXIT. CB FULL (write skipped, Guard 1,
> 6 pending). Reconcile clean (2/2 match); Alpaca-mirror in sync. Broker EOD
> equity $100,189.23 → +0.19% since inception; day −$195.39 (−0.20%
> close-to-close). Drawdown 0.60% from frozen peak $100,792.58. Recurring GLD
> A/C signal-conflict logged (`logs/risk_events/2026-07-16_204500_signal_conflict.md`).
> Regime read shifted to bullish_trend / medium (SPY +1.47% above 50dma) but on
> the same stale bars.

## Resolutions of prior open predictions (from 2026-07-15, window = this EOD)

- **P1 — pending-broker suppression persists until finalizer fix lands (conf 0.9):**
  ✅ CONFIRMED. `pending_broker_count()` still **6**; no PR merged touching the
  fill finalizer. CB write skipped again (Guard 1) this run.
- **P2 — daily-bar feed still broken into 2026-07-16 (conf 0.8):** ✅ CONFIRMED.
  yfinance TLS-blocked (curl 35) across all symbols on the fresh EOD fetch;
  Alpaca IEX daily bars lag (candidates newest short-window 2026-07-10 ~160.8h;
  SPY 300-bar window ends 2026-06-29 ~16.7d). All new ENTRYs NO_TRADE under
  rule #5. Real-time IEX quotes remained available for marks.
- **P3 — circuit-breaker stays FULL (conf 0.9):** ✅ CONFIRMED. CB FULL carried;
  no transition. Diagnostic DD ≈ 0.60% (equity $100,189.23 vs frozen peak
  $100,792.58), ~7.4pp cushion to the 8% HALF trigger.
- **P4 — UNH re-entry stays refused through the 07-16 earnings event (conf 0.7):**
  ✅ CONFIRMED (window still partly open). UNH re-entry REFUSED at this EOD
  (`decisions/2026-07-16/1645_UNH.json`) on BOTH staleness and the 07-16 BMO
  earnings caution — bars are still stale so no post-earnings fresh close exists
  yet; the re-entry decision proper is deferred to the first fresh post-earnings
  EOD.
- **P5 — routine-log archiver keeps erroring until the malformed file is fixed
  (conf 0.85):** ✅ CONFIRMED. `scripts/archive_routine_logs.py` again exited
  non-zero (non-fatal) on the same legacy filename
  `2026-06-01_0935_market_open_noop.md`; no rename/removal PR landed.

## Prediction 1 — pending-broker suppression persists until the finalizer fix lands
- **Prediction (conf 0.9):** `pending_broker_count()` stays ≥ 6 (CB writes stay
  skipped, Guard 1) on the next EOD unless a human PR lands the
  `confirm_broker_fills()` status-normalization fix or an operator manually
  finalizes the stale PENDING_BROKER rows.
- **Resolution window:** next EOD after any PR merge touching `lib/paper_sim.py`
  or `lib/broker.py`, or after a manual operator finalization.

## Prediction 2 — daily-bar feed still broken into 2026-07-17
- **Prediction (conf 0.8):** 2026-07-17 routines again evaluate on stale/failed
  daily bars unless a proxy-reachable fresh daily-bar feed is wired; new ENTRY
  signals stay NO_TRADE under rule #5. Real-time IEX quotes stay available for
  held-position health. This is the single blocking dependency — the book has
  been unable to act on any entry for ~3 weeks (Strategy C overlay stuck at
  0% vs 10% target).
- **Resolution window:** next routine that fetches daily bars.

## Prediction 3 — circuit-breaker stays FULL next session
- **Prediction (conf 0.9):** CB remains FULL with no transition next session.
  DD ≈ 0.60% is ~7.4pp below the 8% HALF trigger and the book is only 2 long
  positions; a same-session move to HALF would require a ~$7.5k drawdown, and
  Guard 1 will most likely keep the write skipped regardless.
- **Resolution window:** next EOD CB step.

## Prediction 4 — GOOGL earnings-date discrepancy resolves outside today's window
- **Prediction (conf 0.7):** GOOGL (rank-5, thinnest-buffer held name) does NOT
  hit its 1-day earnings-caution window before the reported date (news_sentiment
  07-28 AMC vs prior journals 07-22 AMC — to be reconciled). No forced pre_close
  earnings de-risk of GOOGL before whichever date is correct; if bars turn fresh
  and GOOGL slips out of the top-5, a rotation EXIT (not an earnings close) is the
  likelier trigger.
- **Resolution window:** the EOD/pre_close preceding the confirmed GOOGL earnings
  date.

## Prediction 5 — routine-log archiver keeps erroring until the malformed file is fixed
- **Prediction (conf 0.85):** `scripts/archive_routine_logs.py` keeps exiting
  non-zero every routine (non-fatal) until a human renames/removes
  `logs/routine_runs/2026-06-01_0935_market_open_noop.md` to the expected
  pattern, or the archiver is made tolerant of legacy names.
- **Resolution window:** next routine after the file is renamed/removed or the
  archiver is made tolerant.

---

## Weekly reconciliation (appended 2026-07-19 by self_learning; append-only, originals above unchanged)

- **Prediction 4 — GOOGL earnings-date discrepancy resolves outside today's window; no forced pre_close earnings de-risk of GOOGL before the confirmed date (conf 0.7):**
  ✅ CONFIRMED (earnings-date leg). news_sentiment on 07-17 confirmed GOOGL Q2 earnings **2026-07-22 AMC** (`journals/daily/2026-07-17.md` pre-market + midday news overlay), reconciling the prior 07-28-vs-07-22 discrepancy to 07-22 AMC. GOOGL did NOT enter a 1-day earnings-caution window this week and was NOT closed on earnings-caution grounds. Note (separate trigger): GOOGL WAS closed 07-17 midday, but on a NEWS-driven discretionary trigger (Gemini 3.5 Pro delay, Bloomberg 07-16), not an earnings de-risk — see `decisions/2026-07-17/1206_GOOGL.json` and `memory/prediction_reviews/2026-07-17.md` P1. The predicted "rotation EXIT is the likelier trigger if bars turn fresh" branch remains untestable (feed still stale; top-7 rank not recomputable).
