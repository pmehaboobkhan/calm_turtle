# Prediction Reviews — 2026-07-09 (end_of_day)

> Predictions/observations from `end_of_day_2026-07-09` (~16:43 ET). Mode
> `PAPER_TRADING` (learning writes permitted). 0 opens, 0 closes, 0 fresh
> signals (daily-bar feed down → rule #5 HOLD-ALL). CB FULL (write skipped,
> Guard 1, 4 pending). Reconcile clean (4/4 match); Alpaca-mirror in sync.
> Broker equity $100,572.89 → +0.57% since inception; day +$33.00 (+0.03%).

## Resolutions of prior open predictions (from 2026-07-08, window 2026-07-09 EOD)

- **P1 — pending-broker suppression persists until finalizer fix lands (conf 0.9):**
  ✅ CONFIRMED. `pending_broker_count()` still 4; no PR merged touching
  `lib/paper_sim.py`. CB write skipped again (Guard 1). Sim equity $101,867.82
  vs broker $100,572.45 — inflated ~$1,295.
- **P2 — daily-bar feed still broken next session (conf 0.8):** ✅ CONFIRMED.
  2026-07-09 EOD could not evaluate signals on today's close — yfinance
  TLS-blocked (curl 35) across all symbols; Alpaca daily bars still lag to
  ~06-23. New entries NO_TRADE under rule #5. Real-time IEX quotes remained
  available for exit/health checks.
- **P3 — circuit-breaker stays FULL through 2026-07-09 (conf 0.9):** ✅ CONFIRMED.
  CB FULL carried; no HALF transition. Broker equity $100,572.89 vs frozen peak
  $100,792.58 → DD ≈ 0.22%, ~7.8pp cushion to the 8% HALF trigger.
- **P4 — all 4 holds stay above stops; JNJ approaches its take-profit (conf 0.6):**
  ✅ CONFIRMED (stops). `positions_to_close()` = []; no stop breached, no target
  hit. JNJ did NOT reach its +25% take-profit ($290.96) — account marks it well
  short. GOOGL remains the thinnest held name. NOTE: post-close IEX quote marks
  this run were unreliable (aggregate ~$1,295 above account equity), so the
  per-symbol % moves are indicative only; robustness of "zero closes" holds
  because even the inflated marks tripped no stop/target and account equity is
  nowhere near a stop cascade.

## Prediction 1 — pending-broker suppression persists until the finalizer fix lands
- **Prediction (conf 0.9):** `pending_broker_count()` stays at 4 (CB writes stay
  skipped) on the next EOD unless a human PR lands the `confirm_broker_fills()`
  status-normalization fix or an operator manually finalizes the rows.
- **Resolution window:** next EOD after any PR merge touching `lib/paper_sim.py`.

## Prediction 2 — daily-bar feed still broken next session
- **Prediction (conf 0.8):** 2026-07-10 pre_market / EOD again evaluate on
  stale/failed daily bars unless a proxy-reachable, fresh daily-bar feed is
  wired; new ENTRY signals stay NO_TRADE under rule #5. Real-time IEX quotes
  remain available for exit/health checks.
- **Resolution window:** 2026-07-10 EOD.

## Prediction 3 — circuit-breaker stays FULL
- **Prediction (conf 0.9):** CB remains FULL through 2026-07-10; no HALF
  transition. DD from frozen peak ≈ 0.2%; ~7.8pp cushion to the 8% HALF trigger
  even if a CB write resumes.
- **Resolution window:** next session with a written CB mark.

## Prediction 4 — all 4 holds stay above stops; JNJ still shy of take-profit
- **Prediction (conf 0.6):** GOOGL (thinnest), SPY, JNJ, UNH do not breach stops
  on the next fresh session. JNJ has not hit its +25% take-profit ($290.96) and
  is not expected to gap into it before its 07-15 earnings; a further JNJ leg up
  could trigger a target-hit CLOSE on a future close. GOOGL remains the name to
  watch (earnings 07-22, capex/EU-fine bear factors).
- **Resolution window:** 2026-07-10 EOD (fresh-data session).

## Prediction 5 — post-close IEX quote basis-gap is a feed artifact, not real MTM
- **Prediction (conf 0.75):** the ~$1,295 gap between `latest_quotes_for_positions`
  marks and Alpaca account equity is a post-close/ask-side IEX print artifact
  (same basis gap the CB Guard-2 diagnostic surfaces), and will not appear as a
  real reconcile discrepancy — next session's `reconcile()` and mirror stay clean
  and account equity remains the authoritative portfolio value.
- **Resolution window:** 2026-07-10 EOD.
