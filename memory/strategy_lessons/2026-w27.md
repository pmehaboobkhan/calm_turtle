# Strategy Lessons — 2026-W27 (June 29 – July 3, 2026)

> Written by: weekly_review self_learning step (SAFE_MEMORY_UPDATE).
> Mode: PAPER_TRADING (confirmed `config/approved_modes.yaml`; not SAFE_MODE — learning writes permitted).
> Operating mode: **v1 observations-only** — `prompts/proposed_updates/.v2_enabled` is ABSENT.
>   → `max_self_learning_proposals_per_cycle = 0`. **No `prompts/proposed_updates/` files written.**
>   → No strategy/risk review docs written. Candidate fixes are recorded here descriptively only
>     ("observations-only; not actioned in v1"), consistent with the W23 precedent.
> Trading days covered: 2026-06-29 (lost session) → 2026-07-03 (07-03 market CLOSED, July 4 holiday obs.).
> Effective sessions: 06-30 (pre_close + EOD only), 07-01 (full), 07-02 (full), 07-03 (monitoring no-op).
> Realized this week: 3 stop-loss exits on 07-01 (CSCO −$304 / −5.3%, NVDA −$391 / −6.9%,
>   XOM −$419.01 / −10.6%), total ≈ −$1,114. 0 new entries (all blocked by stale daily bars, CLAUDE.md #5).
> Ending book: 4 positions (SPY, GOOGL, JNJ, UNH), unrealized +$1,785.37.

---

## Lesson 1 — MUST-FIX: Total operational blackout, ISO weeks 25–26 (2026-06-15 → 2026-06-26)

**Source (observation):** `journals/daily/` jumps directly from `2026-06-12.md` to `2026-06-30.md`. For the entire 2.5-week span 2026-06-15 → 2026-06-26 there are **zero** daily journals, zero `decisions/<date>/`, zero `logs/risk_events/`, and zero `logs/routine_runs/` entries. The last actually-run `weekly_review` was W23 (`journals/weekly/2026-23.md`, `reports/weekly_digest/2026-23.md`, review date 2026-06-06). ISO week 24 (Jun 8–12) has daily journals but was **never given a `weekly_review`** (no `journals/weekly/2026-24.md`).

**What this means (observation, not blame):** the deterministic engine held a ~7-figure paper book **unmonitored for 2.5 weeks** — no per-position stop enforcement, no circuit-breaker equity marks, no journals, no decisions — purely because no routine session executed. This is an **infrastructure / scheduling** failure, not a strategy or model failure. Nothing in the strategy logic misbehaved; the logic was simply never invoked.

**Assessment:** This is the single biggest finding of the review cycle. `config/routine_schedule.yaml` shows weekday routines as *enabled*, but there is no evidence in `logs/routine_runs/` that the cron/session trigger actually invoked this Claude Code environment during the blackout window. Enabled-in-config is not the same as fired-in-fact.

**Recommended operator action (MUST-FIX; observation-only — no config touched):** verify the scheduled cron / session trigger for weekday routines is actually firing and reaching this environment. Confirm by checking that `logs/routine_runs/` receives a fresh entry per scheduled slot going forward. A liveness/heartbeat check that alerts when *no* routine has run in a rolling 48h weekday window would have surfaced this on ~06-16 instead of at the next human touch. (Candidate; observations-only; not actioned in v1.)

**Counter-hypothesis / what would refute the severity framing:** if `logs/routine_runs/` or an external scheduler log shows the routines *did* fire during 06-15→06-26 but failed silently to write artifacts, then the root cause is a write/commit failure inside the routine, not a scheduling miss — a different fix. Operator should confirm which by inspecting the scheduler's own execution log for that window.

---

## Lesson 2 — MUST-FIX: Circuit-breaker persistence stale since 2026-06-11 (~3.5 weeks); Guard-1 stuck-pending-order suppression

**Source (observation):** `trades/paper/circuit_breaker.json` still shows `updated_at: 2026-06-11` — no fresh CB state has been written for ~3.5 weeks. Cause is a "Guard 1" suppression: the CB step skips its write whenever `paper_sim.pending_broker_count() > 0`. The stuck-pending-broker count grew from **1** (residual 2026-06-08 GLD close) to **4** over this week as the CSCO / NVDA / XOM close rows (07-01) were appended — reconcile is Alpaca-authoritative on positions but does not rewrite the append-only log status strings. Every routine 06-30 → 07-03 hit Guard-1 and skipped the CB write, relying only on ad-hoc read-only DD diagnostics that were never persisted (07-01 DD ~0.72%, 07-02 ~0.34%, 07-03 ~0.29% — all far below the 8% HALF trigger).

**Context (recurrence, not new):** this exact condition has been flagged as a "carried-forward concern" in daily journals since 2026-06-08 without resolution, across multiple weekly-review cycles. It *should* have been raised in the W24 review, had one happened (see Lesson 1). A functionally identical stuck-order backlog was successfully cleared on 2026-06-04 by the operator running `scripts/sync_alpaca_state.py --reset-fresh-start` (see `strategy_lessons/2026-w23.md` §Lesson 5). So a known, proven remedy exists and simply has not been applied.

**Recommended operator action (MUST-FIX; observation-only — no config touched):** run `scripts/sync_alpaca_state.py` (as on 2026-06-04) to clear the 4-order pending-broker backlog and resume CB equity marks. Longer-term candidate: a **stale-pending-order timeout** — auto-clear (or alert on) any pending-broker order older than N sessions so Guard-1 cannot silently suppress CB writes for weeks. (Candidate; observations-only; not actioned in v1.)

**Note on Guard-1 itself:** as in W23, Guard-1 is behaving *correctly* — it never fabricated a state or a transition; it conservatively carried forward the last known CB state. The defect is in pending-broker log hygiene, not the CB logic. The risk it creates is **silent staleness**: DD was low all week so no HALF transition was missed, but the system cannot *prove* that from persisted state because no fresh mark was written.

**Counter-hypothesis / what would refute:** if a fresh CB write appears with `updated_at` ≥ next-session date without any operator sync, then Guard-1 is not the blocker and the stale timestamp had another cause.

---

## Lesson 3 — CARRIED-FORWARD (escalated): daily-bar feed stale has now blocked EVERY new entry for 3+ consecutive weeks

**Source (observation):** all week, yfinance bar fetch was TLS-blocked via the proxy and Alpaca free-IEX daily bars lagged 11–19 trading days (anchored at 2026-06-15/06-16 by week's end). Per CLAUDE.md #5, every fresh ENTRY signal (CSCO, NVDA, XOM re-fired rank-1..5) produced `NO_TRADE (data_stale)`. Intraday IEX *quotes/last-trades* were fresh enough on 07-01 to resolve the three stop exits, but the *bar* path — which drives momentum ranking and every entry — stayed broken.

**Escalation vs prior weeks:** this is no longer a per-session inconvenience. Stale daily bars have now blocked **every new entry for 3+ consecutive weeks** (the W23 lesson already logged four consecutive stale EODs 06-01→06-04; the blackout obscured 06-15→06-26; and 06-30→07-03 continued the pattern). It is now the **single biggest recurring operational-reliability item** in the book — it does not just delay individual trades, it has effectively frozen the entry side of all three strategies. Its priority language is escalated above the "escalation threshold needed" framing of W23: the threshold discussion is moot when the streak is measured in weeks.

**Candidate (observations-only; not actioned in v1):** wire a proxy-reachable fresh daily-bar source (or an alternative provider) as the top operational-reliability priority; and a staleness-streak counter that fires an URGENT operator alert at N≥3 consecutive stale sessions (carried from W23 Lesson 1, still unbuilt). What would refute the priority: a single session where fresh bars arrive and entries resume normally — which has not occurred in 3+ weeks.

---

## Lesson 4 — CARRIED-FORWARD (unresolved): GLD permanent-overlay daily-loss-halt exemption is a policy question

**Source (observation):** per the 07-01 / 07-02 / 07-03 journals, the GLD permanent-overlay-vs-daily-loss-halt conflict persists unchanged. The question — should the `gold_permanent_overlay` (10%, a deliberate crisis hedge / diversifier) be exempt from the all-positions daily-loss halt that would otherwise liquidate it? — remains open from prior cycles.

**Status:** unresolved. This is a **human-PR policy decision** (it concerns `config/strategy_rules.yaml` / `config/risk_limits.yaml` interaction and is explicitly out of scope for this agent to change). Recorded here as a standing carried-forward item so it is not lost again to a blackout. No new evidence this week changed the character of the question.

---

## Lesson 5 — CARRIED-FORWARD (unresolved): earnings calendar still not wired to a deterministic source

**Source (observation):** this week's `fundamental_context` runs in pre_close / EOD used **estimated / uncertain earnings dates**, sourced via web when the proxy was reachable (e.g., XOM confirmed 2026-07-31; UNH/JNJ estimated mid-July; CSCO ~08-12; NVDA ~08-26 — several marked "estimated" with lowered confidence, per `prediction_reviews/2026-06-30.md` Prediction 1, conf lowered to 0.8). This is the same non-deterministic LLM-web-recall process flagged in W23 Lesson 2 (patched manually for ORCL in W23).

**Status:** still unwired. A deterministic `data/earnings_calendar.csv` (or API-driven refresh) would make earnings-caution triggers auditable and independent of LLM recall. No new implementation this week. Recorded as carried-forward. (Candidate; observations-only; not actioned in v1.)

---

## Lesson 6 — CARRIED-FORWARD (unresolved, non-fatal): routine-log archive filename brittleness

**Source (observation):** flagged again in the 06-30 EOD and 07-02 EOD journals: the filename `2026-06-01_0935_market_open_noop.md` breaks `scripts/archive_routine_logs.py --keep-days 30` (the parser does not tolerate the `_noop` suffix / the extra token). Still unresolved; non-fatal (archival only, no trading impact). Recorded as carried-forward so it survives the next review boundary.

---

## Lesson 7 — This week's three stop exits (0/3); XOM confirms the W23 sizing-down lesson

**Source (observation):** on 07-01, three deterministic stop-loss exits fired on fresh intraday IEX prints — CSCO −$304 (−5.3%; `decisions/2026-07-01/0939_CSCO.json`), NVDA −$391 (−6.9%; `.../0939_NVDA.json`), XOM −$419.01 (−10.6%; `.../1534_XOM.json`). Full gate chain APPROVED on each. No new entries offset them (stale bars).

**Observation — NOT a conclusion about strategy quality:** 0 wins / 3 losses is **N=3**. This is far below the N≥20 threshold for any strategy-level calibration claim and below N≥5 even for symbol-specific claims — it is descriptive only and carries no verdict on `large_cap_momentum_top5`. All three were mechanical stop honors, not thesis failures caught late.

**XOM sizing — positive confirmation of an existing lesson (not new drift):** XOM's two paper-period closes are now:
- 2026-05-29: −$1,461 (−9.35%) at **97 sh**, liquidated by the *daily-loss halt* (single-name energy beta exceeded the $500 daily budget). See `symbol_profiles/XOM.md` W22.
- 2026-07-01: −$419.01 (−10.6%) at **26 sh**, closed cleanly by the *per-position stop* — and **−$419 < the $500 daily-loss cap**, so it did NOT trip the daily-loss halt.

W23 Lesson 3 predicted that sizing XOM down to ~26 sh (vs 97) would keep a ~−10% XOM move under the daily-loss cap and let the ordinary stop govern instead of the system-level halt. **That is exactly what happened this week.** This is supporting evidence *for* the existing sizing mitigation, which argues **against** any new risk-rule change. XOM remains a descriptive energy-beta loss generator (0-for-2 closes, both energy-driven), but the mitigation is working as designed.

**Why no risk-rule review doc was written:** (a) v1 observations-only forbids strategy/risk review docs; (b) even setting that aside, N=3 this week is not statistically meaningful drift; (c) the only recurring symbol pattern (XOM energy-beta losses) is already mitigated by an existing lesson that this week *confirmed*, not refuted. Refutation trigger to revisit: XOM produces a *third* energy-beta loss that breaches the daily-loss cap **at the reduced size**, which would indicate the sizing floor is insufficient.

---

## Lesson 8 — NEW (watch-item, not a mistake): GOOGL thin +2.9% stop cushion

**Source (observation):** GOOGL is the only underwater open position at week's end (−7.70%), sitting only **+2.91% above its 331.68 stop** — the thinnest cushion in the 4-name book (`prediction_reviews/2026-07-02.md` P4, `2026-07-03.md` P4). Its fresh ENTRY / momentum thesis is described as intact.

**Framing:** this is a **watch-item**, not a documented mistake. The position has not breached; the thin cushion simply makes it the most likely next stop candidate on a fresh session. Recorded so pre_close on the next trading day (2026-07-06) prioritizes GOOGL for stop-proximity monitoring. Prediction P4 (conf 0.6, GOOGL holds above stop) remains open, resolving 2026-07-06 on fresh quotes.

---

## Lesson 9 — NEW (sub-agent reliability gap): trade_proposal left gate verdicts PENDING on the XOM close

**Source (observation):** on the 07-01 XOM close, the `trade_proposal` sub-agent left the gate verdicts **PENDING** — the orchestrator had to run `risk_manager` and `compliance_safety` explicitly itself to complete the gate chain before executing the (correct) stop exit. The final decision was sound and fully gated; the concern is the *reliability of the sub-agent handoff*, not the outcome.

**Observation (no verdict on the agent):** this is one instance (N=1) of a sub-agent not carrying the gate chain to completion. Recorded descriptively; no "this agent is unreliable" conclusion is drawn on a single occurrence. Worth watching for recurrence — if the orchestrator repeatedly has to backfill gate verdicts for `trade_proposal`, that would become a documented handoff defect. What would refute the concern: the next several close decisions show `trade_proposal` returning completed gate verdicts without orchestrator backfill.

---

## Lesson 10 — NEW (unexplained, lesson-note only): pre_market orchestrator stalled before composing the report on 07-01

**Source (observation):** on 07-01 the pre_market orchestrator stalled before composing its report; the main-loop routine took over synchronously and the session still completed its operative work (the two market_open stop exits executed). Root cause is unclear from the artifacts.

**Framing:** lesson-note only, no config change proposed (and none permitted in v1). Recorded so that a recurrence can be correlated — a single unexplained stall is not actionable, but a second instance with the same signature would justify an operator investigation into orchestrator start-up. No counter-hypothesis can be tested until more instances (or a scheduler log for that run) are available.

---

*This file is append-only. Written: 2026-07-04 (weekly_review self_learning, ISO week 2026-27).*
